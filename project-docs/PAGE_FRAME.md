# PAGE_FRAME

## 文档状态

- 文档状态：Confirmed
- 最近更新时间：2026-03-30
- 最近更新触发器：运行时头部状态切到快照消费，设置页继续压缩布局，需要把当前页面框架同步到工作区真实状态。

## 页面清单

| 页面/视图 | 目标 | 入口 | 当前状态 |
| --- | --- | --- | --- |
| `/` 总览 | 展示任务、素材、市场摘要和主入口分发 | 主导航 `总览` | Confirmed |
| `/create` 创作台 | 创建任务、上传图片、填写商品信息、调用智能体辅助补全 | 主导航 `创作台` | Confirmed |
| `/history` 历史记录 | 查看任务列表、筛选、分页并跳转详情 | 主导航 `历史记录` | Confirmed |
| `/jobs/[id]` 任务详情 | 查看任务、单图状态、审核、下载、重试、飞书同步 | 历史页进入 | Confirmed |
| `/settings` 设置 | 维护模型、Relay、飞书与运行时配置 | 主导航 `设置` | Confirmed |
| `/brands` 品牌库 | 独立维护品牌规则、新建/编辑/删除品牌 | 主导航 `品牌库` | Confirmed |
| `/templates` | 旧模板中心路由 | 无可见导航入口 | As-Built（退役） |

## 全局头部框架

- 顶部第一层：历史摘要条
  - `累计任务 / 累计生成 / 成功任务 / 失败任务`
- 顶部第二层中区：
  - `/create`：`智能体入口 + 主导航`
  - 其它页面：仅主导航
- 顶部右区：
  - `Gemini / 飞书 / LAN` 三态状态芯片
  - 语言切换按钮
- 状态来源：
  - 首屏使用服务端注入的运行时快照种子
  - 前端在可见态轮询 `/api/runtime/header-snapshot`

## 总览页框架

- 总览主容器：
  - 居中的单胶囊面板
  - 中轴标题区
  - 运行状态条
  - `3` 个指标卡
  - `3` 个主入口动作卡
- 首页状态条当前与顶栏共享同一份运行时快照数据源，不再单独做页面级即时探测。

## 设置页框架

- 主体桌面结构：
  - 左上 `Gemini / 中转设置`
  - 左下 `素材与任务`
  - 右侧整列 `飞书多维表格同步`
- 字段密度：
  - Gemini 区为 `3` 列网格
  - 请求头 JSON 跨 `3` 列
  - 飞书连接区为 `3` 列
  - 飞书映射区独立承载模板按钮和 JSON textarea
- 底部动作区：
  - 左侧：紧凑状态反馈
  - 右侧：`全局连接测试`、`保存全部设置`
- 当前页面已不再保留独立 Hero 区。

## 响应式回落

- `<=1023px`
  - 设置页改为单列
  - 顺序固定为 `Gemini / 中转设置 -> 飞书多维表格同步 -> 素材与任务`
  - Gemini 区从 `3` 列回落到 `2` 列
- `<=720px`
  - Gemini 区回落到单列
  - 飞书连接字段回落到单列
  - 底部动作区按钮改为单列

## 导航结构

- 主导航：`总览 / 创作台 / 历史记录 / 设置 / 品牌库`
- 中区结构：
  - `/create`：`智能体入口 + 主导航`
  - 其它页面：仅主导航

## 实际落地偏差

- 对外 `README.md` 与 `Readme/` 当前仍锚定 `v0.8.0` 已发布口径，本文件描述的是本地工作区最新页面事实。
- 当前页面框架结论已覆盖顶栏快照消费与设置页压缩布局，但尚未补浏览器级截图验收。
