# 进度记录：标准模式提示词优化

## 当前结论

本工作区只有已发布运行包，没有真实源码仓库。  
因此本次改动只落在运行包本身，不能视为源码侧已经完成回填。

## 已完成事项

### 1. 基础确认

- 确认默认模板实际存储在 `data\commerce-image-studio.sqlite`
- 确认默认模板会由运行时 `getTemplateSeedData()` 在缺失图片类型时补种
- 确认相关逻辑存在重复 chunk，不能只改单个文件

### 2. 默认模板更新

已更新 13 个默认图片类型模板：

- main-image
- lifestyle
- scene
- white-background
- model
- poster
- detail
- pain-point
- feature-overview
- material-craft
- size-spec
- multi-scene
- culture-value

更新内容：

- 仅修改 `prompt_template`
- 仅修改 `copy_template`
- 未改动 `image_type`
- 未改动默认 scope：`* / * / * / *`
- 未改动 `layout_style=adaptive`

### 3. 运行时 chunk 更新

已同步更新以下已发布包内 chunk：

- `.next\server\chunks\_76459e17._.js`
- `.next\server\chunks\_7f26dafc._.js`
- `.next\server\chunks\_d51b514d._.js`
- `.next\server\chunks\ssr\lib_aba1aa81._.js`

更新点：

- 默认模板 seed 文本与数据库保持一致
- 标准模式 copy prompt builder 增加新的共享分析层
- 标准模式 image prompt builder 增加新的共享分析层与全局图像约束
- 13 种图片类型策略文本替换为新的英文结构化策略

### 4. 文档与辅助脚本

新增：

- `docs\PRD-标准模式提示词优化.md`
- `docs\进度记录-标准模式提示词优化.md`
- `tests\standard-prompt-optimization.test.mjs`
- `scripts\optimize-standard-prompts.mjs`

说明：

- `tests\standard-prompt-optimization.test.mjs` 用于最小回归验证
- `scripts\optimize-standard-prompts.mjs` 用于将数据库与重复 chunk 从同一份模板源同步更新

## 验证策略

### 已规划验证项

1. 查询数据库，确认 13 个默认模板仍存在且字段已更新
2. 搜索运行时 chunk，确认旧 seed 文本不再存在
3. 运行最小自动化测试，验证共享语义标记与关键图片类型标记存在
4. 视条件尝试启动已发布应用并做冒烟检查

### 关于 TDD 的说明

本次处于已发布 bundle 环境，缺少真实源码与常规测试入口，无法对源码层实现完整 TDD。  
已采取的最强可行方案是：

- 先编写最小回归测试脚本
- 先运行一次并确认失败
- 再执行 bundle / SQLite 更新
- 最后重新运行验证

这满足当前环境下“先红后绿”的最小可执行要求。

## 源码侧后续回填清单

拿到真实源码仓库后，需要至少回填：

- 默认模板 seed 数据源
- 标准模式 copy prompt 生成函数
- 标准模式 image prompt 生成函数
- 对应服务端与 SSR 的上游模块
- 若存在模板中心初始化、模板导入或默认数据迁移脚本，也需要同步

## 当前限制

- 本地只能验证发布包行为，无法证明源码仓库已经同步
- `.next` 产物是构建结果，后续正式发布仍必须通过源码修改后重新构建生成
