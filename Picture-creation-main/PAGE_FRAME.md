# PAGE_FRAME

## 文档状态

- 文档状态：As-Built
- 最近更新时间：2026-03-30
- 最近更新触发器：准备发布 `v0.8.0`，将当前导航、设置中心和退役入口固化为发布态页面框架。

## 页面清单

| 页面/视图 | 目标 | 入口 | 当前状态 |
| --- | --- | --- | --- |
| `/` 总览 | 展示任务、素材、市场摘要和主入口分发 | 主导航 `总览` | Confirmed |
| `/create` 创作台 | 创建任务、上传图片、填写商品信息、调用智能体辅助补全 | 主导航 `创作台` | Confirmed |
| `/history` 历史记录 | 查看任务列表、筛选、分页并跳转详情 | 主导航 `历史记录` | Confirmed |
| `/jobs/[id]` 任务详情 | 查看任务、单图状态、审核、下载、重试、飞书同步 | 历史页进入 | Confirmed |
| `/settings` 设置 | 维护模型、Relay、飞书与运行时配置 | 主导航 `设置` | Confirmed |
| `/brands` 品牌库 | 独立维护品牌规则、新建/编辑/删除品牌 | 主导航 `品牌库` | Confirmed |
| `/templates` | 旧模板中心路由 | 无可见导航入口 | As-Built（退役） |

## 设置页框架

- 顶部：
  - 居中 Hero 区
  - 页面标题
  - 一句短说明
- 主体桌面结构：
  - 第一排 `Gemini / Relay` 主卡
  - 第一排右侧 `飞书同步` 侧卡
  - 第二排右侧 `素材与任务` 侧卡
- 页面操作：
  - 底部统一状态区
  - `全局连接测试`
  - `保存全部设置`
- 底部：
  - 薄动作区
  - 承担状态反馈、全局测试与全局保存 CTA

## 响应式回落

- `<=1023px`
  - 设置台改为单列
  - 顺序固定为 `Gemini / Relay -> 飞书同步 -> 素材与任务`
- `<=720px`
  - 飞书连接字段改为单列
  - 卡头与底部动作区允许换行
  - 底部动作区按钮改为单列

## 导航结构

- 主导航：`总览 / 创作台 / 历史记录 / 设置 / 品牌库`
- 中区结构：
  - `/create`：`智能体入口 + 主导航`
  - 其它页面：仅主导航

## 实际落地偏差

- 对外文字口径已同步到 `v0.8.0`
- 当前截图资产仍沿用上一轮拍摄批次，后续可按需补拍
- 当前页面框架事实以源码和本文件为准
